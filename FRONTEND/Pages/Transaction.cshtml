@page "{handler?}"
@model FrontEnd.Pages.TransactionModel
@{
}
@Html.AntiForgeryToken()
<div class="row">
    <div class="col-lg-12">
        <div class="card ribbon-box">
            <div class="card-header d-flex align-items-center">
                <h5 class="card-title mb-0 flex-grow-1">Transaction List</h5>
            </div>
            <div class="card-body">
                <div id="roleGrid"></div>
            </div>
        </div>
    </div>
</div>
@section scripts{
    <script type="text/javascript">
        var $users = @Html.Raw(Json.Serialize(Model.AllDdl?.UserList)), $masters = @Html.Raw(Json.Serialize(Model.AllDdl?.MasterList));
    </script>
    <script src="~/js/onetomany-page.js"></script>
}
